# FN 작업완료보고

> 매 사이클 완료 후 무엇이 업데이트됐는지 맨 위에 누적 기록.

## [FN-사이클 4] 2026-07-04 — 실시간 WebSocket 캔들 갱신
- 산출물: A(BN동기화) + C(통합)
- BN 스펙 diff: 신규 `WS /ws/ticker` 반영.
- 업데이트: `config.js`에 BN_WS(https→wss) 추가. App에서 `WS /ws/ticker?market=` 접속 → 체결가로 **형성 중 마지막 봉을 실시간 update()**(시간버킷 계산으로 새 봉 롤오버 처리). 현재가 헤더가 실시간 체결가 우선. 상단 "● 실시간" 배지(WS 연결 표시). 마켓 변경 시 재연결.
- 검증: 배지 ● 실시간(WS onopen), 라이브 가격 표시(BTC 94,269,000), 마켓 전환 재연결(DOGE) 확인. 콘솔 에러 0. (가격 미변동 구간은 체결 없음일 뿐 — 소켓 수신은 파이썬/터널 wss로 별도 검증됨)
- 커밋: (아래 push)

## [FN-사이클 3] 2026-07-04 — 백테스트 요약 패널 + BN /api/backtest 연동
- 산출물: A(BN동기화) + C(통합)
- BN 스펙 diff: 신규 `GET /api/backtest` 반영.
- 업데이트: 하단에 **백테스트 패널** 추가(거래수·승률·총수익(색상)·손익비 PF·MDD, "비용차감" 표기). load()에 `/api/backtest` 병렬 fetch 추가. 왕복거래 0건이면 매수/매도 신호 수 안내.
- 검증(DOM): BTC 거래수 2·승률 0%·**총수익 -0.905%**·MDD -0.905% 표시, BN stage 3-backtest 연결, 콘솔 에러 0.
- 커밋: (아래 push)

## [FN-사이클 2] 2026-07-04 — 참고 UI(STOCKPIKING) 적용 + BN 지표·신호 연동
- 산출물: A(BN동기화) + B(구축) + C(통합)
- BN 스펙 diff: 신규 `GET /api/indicators`, `GET /api/signals` 반영.
- 업데이트:
  - 레이아웃 전면 개편(참고 UI 구조 적용): 상단바 + **사이드바 관심종목(BTC/ETH/XRP/SOL/DOGE)** + 메인 차트 + **하단 지표요약·신호로그 패널**. 참고 토큰(--r:2px·1px 실선·라이트/다크).
  - 차트: 캔들(상승 빨강/하락 파랑) + **EMA20/EMA50/VWAP 지표선 오버레이**(토글) + **매수▲/매도▼ 신호 마커** + 거래량.
  - BN 연동: `/api/indicators`(지표선·요약), `/api/signals`(마커·로그)를 병렬 fetch(4상태 처리).
  - 다크모드 토글(localStorage), 모바일 반응형(사이드바 가로 스크롤).
- 생성 페이지/버튼: 관심종목 5개·지표 토글 3개·분봉 탭 6개·다크토글·새로고침.
- 검증: 프리뷰(desktop) 렌더 정상 — RSI 43.3·EMA20/50·VWAP 표시, 신호로그 14개(매도 마커), BN stage 2-signals 연결. 콘솔 에러 0.
  - 미검증: mobile/tablet 실기기·다크 상세.
- 커밋: (아래 push)

## [FN-사이클 1] 2026-07-04 — React 대시보드 구축 + BN 연동 + GitHub Pages 배포
- 산출물: A(BN동기화) + B(구축) + C(통합)
- BN 스펙 diff: 신규 `GET /health`, `GET /api/candles` 반영(최초).
- 업데이트:
  - Vite React 스캐폴딩(package.json·vite.config·index.html·main.jsx).
  - `App.jsx` 대시보드: BN `/health` 연결배지·모드배지, `/api/candles` 비동기 로드(loading/ok/empty/error 4상태), lightweight-charts 캔들+거래량, 마켓/분봉 셀렉터·새로고침 버튼.
  - `config.js`: BN_URL을 COIN_BN README의 Cloudflare 터널 주소로 설정.
  - `styles.css`: KRDS 토큰(border-radius 0·1px 실선·무채색), 상승=빨강/하락=파랑.
  - GitHub Pages 배포(base `/COIN_FN/`, gh-pages 브랜치).
- 생성 페이지/버튼: 대시보드 1페이지 · 마켓·분봉 select · 새로고침 버튼 · 연결/모드 배지.
- 검증: 프리뷰(desktop) 렌더 정상 — 실 BTC 캔들(94,309,000 KRW)·상승빨강/하락파랑·"BN 연결됨/paper". BN 터널 실연동 확인. 콘솔 에러 0.
  - 미검증: mobile/tablet·다크모드(다음 사이클).
- BN 요청: 실시간 WS(현재가 push), 포지션/잔고 엔드포인트(로드맵 진행 시).
- 커밋: (아래 push)
